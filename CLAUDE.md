# CLAUDE.md

Конституція цього репо — [`AGENTS.md`](AGENTS.md). Читай його першим: місія,
принципи (S1–S5, P1–P10, C1, D1), архітектурна мапа, операційний мануал і guardrails.

Цей файл — лише вказівник (P7: один факт — одне місце). Roadmap-и та doc-map — в
AGENTS.md §7.

Коротке нагадування найчастіших правил:
- Тести й mypy ганяються **по-сервісно** (`pytest gateway/tests`, `mypy gateway/app`, …).
- Секрети лише в `.env`; нова фіча — за прапором із безпечним дефолтом.
- Conventional commits; гілка від `main`, не комітити в `main` напряму.
- Закрив задачу → `[x]` у відповідному roadmap **у тому ж PR** (D1 doc-code sync).

## Підключення до Claude / Jarvis

Локальна пам'ять користувача (кнопка «Пам'ять» у десктоп-віджеті Jarvis; у треї —
«📖 Відкрити пам'ять») зберігається у `C:\Users\<користувач>\Jarvis\memory\`.
Якщо сесія йде на машині користувача — на старті переглянь цю теку й використовуй
збережені нотатки як контекст користувача. У хмарних/CI-сесіях тека недоступна —
просто пропусти цей крок.
